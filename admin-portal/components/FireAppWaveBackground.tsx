'use client';

/**
 * Wave backdrop was removed from login. This is a no-op placeholder so a stale
 * dev cache that still resolves this module does not fail the build.
 * You can delete this file after `rm -rf .next` and a fresh `npm run dev`.
 */
export function FireAppWaveBackground() {
  return null;
}
